# Core Rules (Canonical location)

The canonical repository invariants now live at:

- `docs/ledgers/CORE_RULES.md`

Treat that file as binding for repo-wide core runtime and architecture constraints.
